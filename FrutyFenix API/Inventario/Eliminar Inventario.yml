info:
  name: Eliminar Inventario
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/inventario/ID_AQUI"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
