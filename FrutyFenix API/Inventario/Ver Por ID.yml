info:
  name: Ver Por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/inventario/ID_AQUI"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
