info:
  name: Ver Por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/proveedores/ID_AQUI"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
