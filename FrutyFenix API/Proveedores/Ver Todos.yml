info:
  name: Ver Todos
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/proveedores"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
