info:
  name: Eliminar Proveedor
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/proveedores/ID_AQUI"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
